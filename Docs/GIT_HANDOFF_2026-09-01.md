# Git引渡し票（2026-09-01）

## 結論

正本はこの文書と`pom.xml`があるリポジトリ直下、現在のブランチは`master`です。変更は未コミットで、GitHub・Renderへは反映していません。現在の作業ツリーを破棄せず、公開前の運用情報を確定してからレビュー、commit、pushへ進んでください。

## Git状態

- HEAD: `241b4692ad2d5ce4525136e3023151de348b3e7d`（セキュリティ設定の非推奨警告を解消）
- origin: `git@github.com:OkuyamaTakaki/springboot-diary-app.git`
- ローカルの `origin/master` 追跡参照: HEADと同一、進み0・遅れ0
- 今回は `fetch` していないため、GitHub上の最新状態は未照合
- 追跡済み変更: 25ファイル（2026-09-02再点検）
- 新規ファイル: 21ファイル（2026-09-02再点検）
- `target/`、`work/`、ローカルDB、IDE生成物は `.gitignore` の対象

2026-09-02の読み取り専用再点検:

- 状態は46件（追跡済み変更25、新規21）、ステージ済み0件
- 変更候補に50 MB以上のファイル、`git diff --check`違反は0件
- 秘密情報検査で実秘密は0件。検出は環境変数参照のDBパスワード欄、空のテストDB欄、画面表示文言、コード上の変数名のみ
- `Docs/RELEASE_DECISION_JA.md`を追加し、公開前に人がプライバシー・運用・権利・公開先を判断できる
- Git所有者安全確認は、読み取りコマンドだけへの一時指定で通過させた。PC全体のGit設定は変更していない

## 変更の要点

- 日付検索の条件保持、0件表示、ページ補正、感謝表現の否定判定、編集時の所有者・CSRF確認を改善。
- 削除機能は追加せず、登録・閲覧・検索・並べ替え・編集を維持。
- 日本語／English切替、未対応言語の日本語復帰、自己配信フォント、端末幅・safe-area・キーボードフォーカスを追加。
- 認証前の `/privacy`、日英のプライバシー文面、安全な問い合わせURL制限、各画面の導線を追加。
- セキュリティ、所有者分離、入力検証、言語、プライバシーの回帰テストと日本語資料を追加。

主な新規ファイルは、`AGENTS.md`、`Docs/`の検証・プライバシー・ライセンス資料、`LocaleConfig.java`、`LocalizedMessages.java`、`PrivacyController.java`、日英メッセージ、プライバシー画面、共通フッター、自己配信フォント、関連テストです。

## 直近の検査証拠

統括の通常Windows環境で最新版を再実行し、[`VERIFICATION_2026-09-01.md`](VERIFICATION_2026-09-01.md) に合格証拠を更新しました。

- Java 21.0.12
- `.\mvnw.cmd -o -T 1 clean verify`
- 31件成功、失敗0、エラー0、スキップ0、`BUILD SUCCESS`
- 実行JAR生成、日本語・英語キー整合、セキュリティ、プライバシー、代表画面幅を確認済み

## 公開前のプライバシー残件

次の3項目を運営者が確定するまでは公開しません。

1. 利用者が非公開で連絡できる問い合わせ先
2. RenderとNeonの実際の保存地域・利用プラン
3. 削除依頼、バックアップ、アクセスログの具体的な保存期間と対応担当

確定後、Renderの `APP_PRIVACY_CONTACT_URL` を設定し、日英文面を実運用へ合わせ、本番HTTPSでCookie属性・問い合わせリンク・最新規約を確認します。詳細は [`PRIVACY_RELEASE_CHECKLIST.md`](PRIVACY_RELEASE_CHECKLIST.md) を参照してください。

## 統括への引渡し

- commit候補要約: `日英表示とプライバシー案内を含む製品品質改善`
- commit前に新規21ファイルと追跡済み変更25ファイル、秘密情報、フォント許諾文、公開用プライバシー文面を人が確認する。
- GitHubの最新値を取得して差分を再確認した後、統括の公開前ゲートでcommit／push／Render反映を判断する。
- この票の作成時点では、Git設定変更、add、commit、push、Render更新、本番データ操作を行っていない。
