# 最終確認記録（2026-09-01、2026-09-02再確認）

## 2026-09-02 仮問い合わせ窓口の再検証

- 公開済みGitHubプロフィールを仮問い合わせ窓口として表示し、正式窓口ではないこと、秘密情報・個人情報を送らないことを日本語・Englishで明記した。
- 正式窓口は`APP_PRIVACY_CONTACT_URL`で差し替えられ、不正なURLスキームをリンクにしない既存防御を維持した。
- Java 21.0.12、Mavenオフライン・単一ワーカーで`verify`を実行した。
- 31テスト、失敗0、エラー0、スキップ0。実行JAR生成成功。
- 外部通信、Render変更、本番DB操作、日記投稿は実施していない。

## 結論

Java 21.0.12で、言語・レスポンシブ・プライバシー改善とRender起動待ち案内を含む最新版のビルドと自動テスト31件が成功しました。静的検査とローカル画面確認も成功しています。現状はローカル公開候補であり、実機要件を含む公開判定は未完了です。公開、Git push、本番DB操作は行っていません。

## 実行した確認

- `./mvnw -o -T 1 verify`: 31件成功、失敗0、エラー0、スキップ0、`BUILD SUCCESS`
- 実行JAR: `target/myapp-0.0.1-SNAPSHOT.jar`、61,170,404 bytes、SHA-256 `B59DDDFE0419AF9B754A9A3324ACDEFA693D6681AA18C26611BF976303B0B4AA`
- 日本語・英語の肯定的な感謝表現を受理し、両言語の否定表現を拒否
- `ungrateful`、`Thanksgiving`、`unhelped`など、英語キーワードを別単語の一部として誤検出しないことを確認
- 日本語・英語の翻訳キー: 不足0、余分0、空文字0
- 未認証画面と認証済み日記画面: 日本語・英語のテンプレート描画成功
- 画面幅: 基準版の1280×800、375×812に加え、最新版の360×800、390×844、844×390、768×1024、1024×768、1366×768
- ログイン、登録、プライバシー、認証済み日記を390×844、844×390、768×1024、1366×768の日本語・英語で再確認し、横スクロール0、44×44px未満の表示中操作対象0件
- 言語保存: `diary-language=en`、有効期間1年、`SameSite=Lax`
- 自己配信フォント: HTTP 200、3,389,792 bytes、ブラウザ読込成功
- ブラウザのコンソール警告・エラー: 0件
- CSP: 外部フォント許可を除去し、`font-src 'self'`
- `git diff --check`: 問題0件
- Google Fonts実行時参照: 0件
- `TODO`／`FIXME`、競合マーカー、行末空白: 0件

## 最新版の追加確認

- ログイン画面: Androidスマートフォン、iPhone、iPad、Windows PC相当の縦横6サイズで横スクロール0、44px未満の主要操作0件。
- 登録画面: 360×800、844×390、768×1024、1366×768で横スクロール0、44px未満の主要操作0件。明るい緑の登録画面を維持。
- `?lang=fr` のような未対応言語は日本語へ戻し、Cookieも対応言語へ補正。
- 言語切替時に `page`、`sort`、`searchDate` など現在の検索・一覧条件を保持。
- 長い利用者名と競合する中幅の言語切替を修正し、再確認した全サイズで日本語57×44px、English 63×44pxを確保。
- `viewport-fit=cover`、safe-area余白、動的ビューポート高、960px以下のカード表示、長い名前の折り返しを確認。
- ローカル起動はメモリ内H2で成功し、`/health` はHTTP 200。本番DB、既存ローカルDB、外部サービスは未変更。
- ブラウザコンソールの警告・エラー0件。キーボードフォーカスの3px表示をCSSと入力欄で確認。
- 入力エラー後も新規作成フォームを開いたまま題名・本文・日付を保持し、通知を閉じると本文へフォーカスが戻ることを確認。
- 編集完了通知へプレースホルダーではなく実際の題名が表示されることを確認。
- `/privacy`: 1280×720、390×844、844×390で横スクロール0。日本語・英語、公開前警告、ログインへの戻り、各画面からの導線、44px以上の操作領域を確認。
- 画面確認用のローカルサーバーとブラウザタブは終了し、Javaプロセス0件を確認。

実機ではなくCodex内蔵Chromiumの画面幅シミュレーションです。認証済み日記画面は外部へ接続しないメモリ内H2と合成テスト入力だけで代表操作を確認し、サーバー終了時に一時データが破棄されたことを確認しました。iPhone/iPadの実Safari、Android WebView、ノッチ・ホームインジケーター、物理タッチ、実機キーボード操作、200%文字拡大、継続30 FPS、80台以上の物理端末は未確認です。このため実機を含む製品完了とは判定しません。

## 最新版の全テスト結果

統括の通常Windows環境で、ネットワーク取得を行わないオフラインモードと1スレッドを指定して再実行しました。

```powershell
.\mvnw.cmd -o -T 1 verify
```

未対応言語、認証前のプライバシー画面、問い合わせURLの安全な方式制限、Render起動待ち案内を含む31件が合格し、失敗・エラー・スキップ0件、実行JAR生成成功、Javaプロセス終了を確認しました。隔離実行ユーザーで起きたMavenキャッシュへのアクセス拒否は、製品不具合ではなく環境差の診断履歴として扱います。

## セキュリティとデータ保護

- CSRF、認証、所有者照合、CSP、Referrer-Policy、Permissions-Policy、COOPの回帰テストを維持。
- `/privacy` は認証前に日英両方で表示でき、未設定の問い合わせ先を公開リンクにしないことを確認。
- 言語Cookieへ保存するのは `ja` または `en` だけで、個人情報・日記・認証情報を含めない。
- 翻訳表とフォントはローカルに同梱し、表示時に外部サービスへ本文や操作情報を送信しない。
- ブラウザ確認用JARはメモリ内H2だけを開き、合成利用者と合成日記による登録・検索・編集・入力エラー復帰を確認しました。利用者の実データは入力せず、外部投稿、本番DB、既存ローカルDB、外部サービスは変更していません。確認サーバー終了時に一時データは破棄されています。

## 再確認手順

1. Java 21を確認する。
2. 依存取得済みなら `./mvnw -o -T 1 verify`、初回取得が必要なら `./mvnw -T 1 verify` を実行する。生成物を削除してよいことを確認済みの場合だけ、必要に応じて `clean` を追加する。
3. `./mvnw spring-boot:run` で起動する前に、使用するDB設定を確認する。
4. `/login` と `/register` を日本語・英語、PC幅・375px幅で確認する。
5. `/privacy` を日本語・英語で開き、問い合わせ先の設定状態と各画面の導線を確認する。
6. テスト用利用者で `/` を開き、日記入力、検索、並べ替え、編集、通知を両言語で確認する。

## 残る外部作業

- Gitのコミット・push、Render公開、本番DB操作、iCloudバックアップは未実施。
- 実Safari／Android WebView／物理タッチ／実機キーボード、200%文字拡大、継続30 FPSを公開直前に確認する。
- 共通基準の製品完了を名乗る場合は、条件を満たす80台以上の物理端末で検証記録を残す。
- 旧復元物の役割と安全な復元方法は [`LOCAL_ARTIFACT_INVENTORY.md`](LOCAL_ARTIFACT_INVENTORY.md) を参照する。
